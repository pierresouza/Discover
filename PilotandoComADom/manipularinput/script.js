// Manipulando conteúdo
// value
const element = document.querySelector("input");
console.log(element.value); // mostra o valor que já foi incluído no input pela tag value
element.value = "valor que eu quiser"; //atribui o valor que é inserido no input
